spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:13306/bancassurance?serverTimezone=UTC&characterEncoding=UTF-8}
    username: ${DB_USERNAME:banca}
    password: ${DB_PASSWORD:banca}
  jpa:
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
